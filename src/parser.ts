import { Strategy } from './types/strategy'

export const toHTML = (code: string, strategy: Strategy = 'NONE') => {
  return code
}

export const toReactHTML = (code: string, strategy: Strategy = 'NONE') => {
  return code
}

export default toHTML
